#include<cstdio>
#include<cstdlib>
#include<cstring>
#include<cmath>
#include<ctime>
#include<cctype>
#include<climits>
#include<iostream>
#include<string>
#include<sstream>
#include<vector>
#include<map>
#include<list>
#include<queue>
#include<deque>
#include<algorithm>
#include<numeric>
#include<utility>
#include<complex>
#include<memory>
#include<functional>

#define FOR(i,a,b) for( int i = (a) ; i < (b) ; i++ )
#define REP(i,a) FOR(i,0,a)
#define BACK(i,a,b) for( int i = (b)-1 ; i >= (a) ; i-- )
#define BACK_REP(i,a) BACK(i,0,a)

typedef long long ll;
using namespace std;

int main(){
	
	return 0;
}
